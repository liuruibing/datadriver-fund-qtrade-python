"""Chanlun core adapter layer.

The primary algorithm modules remain at ``backend/ZS_sig.py`` and
``backend/BS_sig.py``. This package provides structured import surfaces,
shared runtime setup, data access helpers, and API regression guards around
those core modules.
"""
